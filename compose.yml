# This file is auto-generated by openmediavault (https://www.openmediavault.org)
# WARNING: Do not edit this file, your changes will get lost.

# dosbox-tv
# 

services:
  dosbox-tv:
    image: dosbox-tv:local
    container_name: dosbox-tv
    network_mode: host
    ipc: host
    security_opt:
      - apparmor:unconfined
    cap_add:
      - SYS_ADMIN
      - MKNOD
    device_cgroup_rules:
      - 'c 13:* rmw'
    devices:
      - /dev/dri:/dev/dri
      - /dev/uinput:/dev/uinput
    environment:
      - PUID=1000
      - PGID=100
      - RENDER_GID=992
      - TZ=America/Bahia
    volumes:
      - /docker/dosbox-tv/home:/home/lizard
      - /srv/mergerfs/mediapool/data/games-dos:/games
      - /dev/input:/dev/input
    restart: unless-stopped
